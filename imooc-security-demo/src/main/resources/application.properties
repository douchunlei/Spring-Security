spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/guowang_demo?useUnicode=true&characterEncoding=utf-8&useSSL=false
spring.datasource.username=root
spring.datasource.password=root

#spring-session 存储类型
spring.session.store-type=none

server.port=8060

#security.basic.enabled=false

#imooc.security.browser.loginPage = /demo-signIn.html

#imooc.security.browser.loginType = REDIRECT

#验证码应用级配置
#验证码基本参数(宽度，高度,验证码长度)
imooc.security.code.image.length = 4
#imooc.security.code.image.width = 100
#验证码拦截接口
#imooc.security.code.image.url = /user,/user/*
#验证码生成逻辑